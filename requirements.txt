# ========================================
# Core dependencies (跨平台通用)
# ========================================
numpy>=1.19.0
scipy>=1.5.0
scikit-learn>=0.24.0
pandas>=1.3.0
matplotlib>=3.3.0
seaborn>=0.11.0
ucimlrepo>=1.0.0
tqdm>=4.60.0

# ========================================
# PyTorch — 根据服务器环境选择安装方式
# 不要写死在 requirements.txt，见下方说明
# ========================================
# torch>=2.0.0   ← 注释掉，手动安装（见下）

# ========================================
# LLM 支持
# ========================================
transformers>=4.36.0
accelerate>=0.26.0       # 多GPU / CPU offload 必需
huggingface-hub>=0.20.0

# ========================================
# macOS ONLY — 服务器上不要安装！
# mlx>=0.5.0
# mlx-lm>=0.5.0
# ========================================