# Contextual Bandit Benchmark Datasets 示例

本文整理四个常见 neural contextual bandit benchmark 数据集：

- Statlog (Shuttle)
- Magic Gamma Telescope
- Covertype
- MNIST

这些数据在 neural bandit 论文中通常被转换为 **contextual bandit problem**：  
每个样本的 feature 作为 **context**，类别 label 作为 **正确 arm**。

参考论文：Neural Contextual Bandits with Deep Representation and Shallow Exploration (ICLR 2022)。 :contentReference[oaicite:0]{index=0}

Statlog、Magic、Covertype 和 MNIST 是四个常见的机器学习分类数据集，很多 contextual bandit 论文会将它们转换成 bandit 环境进行实验。在这些数据集中，每一个样本本质上都是一个特征向量 (x)，同时对应一个类别标签，而在 bandit 实验中通常将特征向量作为 **context**，类别作为 **正确的 arm**，通过预测是否正确来获得奖励。论文实验中也给出了这四个数据集的基本规模信息。 

Statlog（Shuttle）数据集来自 UCI Machine Learning Repository，用于航天飞机状态分类任务。每个样本包含 9 个传感器特征，用来描述航天飞机运行状态，目标是将样本划分为 7 种不同的状态类别，总共有约 58,000 条数据。Magic Gamma Telescope 数据集同样来自 UCI，它来源于高能物理望远镜观测数据，用于区分 γ 射线事件和 hadron 事件。每个样本由 11 个图像统计特征构成，用来描述粒子在望远镜中的观测形状和能量分布，共有 2 个类别、约 19,020 条样本。

Covertype 数据集是森林覆盖类型预测数据集，用于根据地理和环境特征判断某片区域的森林类型。其特征包括海拔、坡度、方向、与水源的距离以及土壤类型等，共 54 个属性，需要在 7 种森林类型中进行分类，总样本量约为 581,012 条。MNIST 则是经典的手写数字识别数据集，每个样本是一张 (28\times28) 的灰度图像，通常展开为 784 维向量，目标是识别 0–9 共 10 个数字类别，数据集中包含约 60,000 个训练样本。

在 contextual bandit 实验中，这些分类数据通常被转化为在线决策问题：算法在每一轮看到一个样本特征作为 context，然后选择一个 arm（即预测类别），如果预测的 arm 与真实标签一致则得到 reward=1，否则 reward=0。这样就可以把传统分类数据转化为 sequential bandit 交互环境，从而用于评估 LinUCB、NeuralUCB、NeuralTS 或 Neural-LinUCB 等算法的累计 regret 表现。


# Neural Contextual Bandit 论文 Baselines 与参数设置整理

## 1. 实验使用的数据集

| Dataset | Feature Dim | Arms | Instances |
|-------|-------------|------|-----------|
| Statlog | 9 | 7 | 58,000 |
| Magic | 11 | 2 | 19,020 |
| Covertype | 54 | 7 | 581,012 |
| MNIST | 784 | 10 | 60,000 |

这些数据集被转换为 contextual bandit：
- 每个样本 = context feature
- 每个类别 = arm
- reward = 1 (预测正确) / 0 (预测错误)

实验轮数：

T = 15000 rounds

实验重复：

10 runs 平均结果

来源：Neural-LinUCB ICLR2022 实验部分。

# 2. 所有论文使用的 Baseline 算法

| Algorithm | 类型 | 论文来源 | 主要特点 |
|-----------|------|----------|-----------|
| UCB1 | Multi-armed bandit | Auer 2002 | 无context |
| LinUCB | Linear contextual bandit | Chu 2011 | 线性reward模型 |
| NeuralUCB | Neural contextual bandit | Zhou 2020 | NN + UCB |
| NeuralTS | Neural contextual bandit | Zhang 2020 | NN + Thompson Sampling |
| NeuralLinear | Neural representation + Bayesian linear | Riquelme 2018 | 表征学习 + Bayesian linear |
| Neural-LinUCB | 本文算法 | Xu 2022 | deep representation + shallow exploration |

# 3. 所有算法参数设置

## UCB1

决策公式

a_t = argmax (mean_reward + sqrt(2 log t / n_a))

参数

无可调参数

用途

纯bandit baseline

## LinUCB

reward model

r(x,a) = x^T θ

选择策略

a_t = argmax (x^T θ + α sqrt(x^T A^{-1} x))

参数

λ = 1
α = 0.02

模型

disjoint LinUCB
(每个arm一个模型)

## NeuralUCB

reward model

r(x) = f(x; w)

exploration

UCB on gradient features

矩阵

Z_t = ∑ ∇f(x_i) ∇f(x_i)^T

参数

λ = 1
α = 0.02

训练

SGD
learning rate = 1e-5
iterations = 1000

## Neural Thompson Sampling (NeuralTS)

reward model

r(x) = f(x; w)

exploration

posterior sampling

θ ~ N(μ, Σ)

参数

σ² prior
network learning rate = 1e-5
iterations = 1000

# 4. 神经网络结构

## UCI 数据集 (Statlog / Magic / Covertype)

L = 2 hidden layers

m = 100
k = 100

网络

W1 ∈ R^(100 × d)
W2 ∈ R^(100 × 100)
θ  ∈ R^(100)

## MNIST

L = 3 hidden layers

m = 100
k = 100

网络

W1 ∈ R^(100 × d)
W2 ∈ R^(100 × 100)
W3 ∈ R^(100 × 100)
θ  ∈ R^(100)

# 5. 训练参数

SGD 设置

learning rate = 1e-5
max iterations = 1000

early stopping

loss difference < 1e-6

# 6. Neural-LinUCB 特有参数

regularization

λ = 1

exploration

α = 0.02

network update

H = 100 rounds

每100轮重新训练神经网络

# 7. 统一实验设置

| Parameter | Value |
|----------|-------|
| T | 15000 |
| λ | 1 |
| α | 0.02 |
| learning rate | 1e-5 |
| SGD steps | 1000 |
| NN update | every 100 rounds |
| repetitions | 10 |

# 9. 论文实验结论

1. Neural bandit 方法整体优于 LinUCB
2. Neural-LinUCB 与 NeuralUCB regret 相近
3. Neural-LinUCB 计算更高效

