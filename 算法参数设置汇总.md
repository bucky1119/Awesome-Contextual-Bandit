# 算法参数设置汇总

本文档整理当前代码中“已注册/可选”的算法参数配置，以及 OurMethod 的完整参数链路。

## 1. 注册入口总览

- 入口 A: `run_dataset_step.py`
  - 数据集注册: `newsgroups`, `statlog`, `ag_news`
  - 基线算法注册: `neural_bandit`, `neural_linear`, `ucb1`, `linucb`, `epsilon_greedy`, `neural_ucb`, `neural_linucb`
  - OurMethod 由 `ourmethod` 子命令触发，最终调用 `OurMethod/run_full_experiment.py::run_our_method_pipeline`

- 入口 B: `run_all_experiments.py`
  - 算法白名单: `neural_bandit`, `neural_linear`, `uniform`, `linear_full_posterior`, `bootstrapped_bnn`, `fixed_policy`, `ucb1`, `linucb`, `epsilon_greedy`, `neural_ucb`, `neural_linucb`

---

## 2. 基线算法参数（入口 A: run_dataset_step.py）

### 2.1 neural_bandit

- 代码配置:
  - `context_dim`: 数据集上下文维度
  - `num_actions`: 动作数
  - `layer_sizes`: `[100, 100]`
  - `activation`: `"relu"`
  - `initial_lr`: `0.001`
  - `batch_size`: `512`
  - `init_scale`: `0.3`
  - `use_dropout`: `False`
  - `dropout_rate`: `0.1`
  - `layer_norm`: `False`
  - `verbose`: `False`

### 2.2 neural_linear

- 代码配置:
  - DNN 主体参数与 `neural_bandit` 相同
  - 额外线性后验参数:
    - `lambda_prior`: `0.25`
    - `a0`: `6`
    - `b0`: `6`
    - `training_freq`: `100`
    - `training_freq_network`: `100`
    - `training_epochs`: `100`
    - `initial_pulls`: `2`

### 2.3 ucb1

- 代码配置:
  - `num_actions`: 动作数
  - `alpha`: `2.0`

### 2.4 linucb

- 代码配置:
  - `num_actions`: 动作数
  - `context_dim`: 数据集上下文维度
  - `alpha`: `1.0`
- 算法类默认还支持:
  - `lambda_prior` 默认 `1.0`（当前入口未显式传，走类默认）

### 2.5 epsilon_greedy

- 代码配置:
  - `num_actions`: 动作数
  - `epsilon`: `0.1`
  - `epsilon_decay`: `0.0`

### 2.6 neural_ucb

- 代码配置:
  - DNN 主体参数:
    - `context_dim`, `num_actions`
    - `layer_sizes`: `[100, 100]`
    - `activation`: `"relu"`
    - `initial_lr`: `0.001`
    - `batch_size`: `512`
    - `init_scale`: `0.3`
    - `use_dropout`: `False`
    - `dropout_rate`: `0.1`
    - `layer_norm`: `False`
    - `verbose`: `False`
  - UCB 参数:
    - `lambda_prior`: `1.0`
    - `exploration_bonus`: `0.1`（注意：类内实际使用的是 `alpha`，这里配置的是 `exploration_bonus`）
    - `training_freq`: `50`
    - `training_epochs`: `50`

### 2.7 neural_linucb

- 代码配置:
  - DNN 主体参数:
    - `context_dim`, `num_actions`
    - `layer_sizes`: `[100, 100]`
    - `activation`: `"relu"`
    - `initial_lr`: `0.001`
    - `batch_size`: `512`
    - `init_scale`: `0.3`
    - `use_dropout`: `False`
    - `dropout_rate`: `0.1`
    - `layer_norm`: `False`
    - `verbose`: `False`
  - UCB 参数:
    - `lambda_prior`: `1.0`
    - `alpha`: `0.5`
    - `training_freq`: `50`
    - `training_epochs`: `50`

---

## 3. 额外算法参数（入口 B: run_all_experiments.py）

以下是该脚本支持但入口 A 未注册的算法参数：

### 3.1 uniform

- `num_actions`

### 3.2 linear_full_posterior

- `context_dim`
- `num_actions`
- `lambda_prior`: `0.25`
- `a0`: `6`
- `b0`: `6`
- `initial_pulls`: `2`

### 3.3 bootstrapped_bnn

- `context_dim`
- `num_actions`
- `training_freq`: `100`
- `training_epochs`: `100`
- `q`: `10`（bootstrap 模型数）
- `p`: `0.8`（样本进入每个模型的概率）
- `initial_pulls`: `2`
- `buffer_s`: `10000`

### 3.4 fixed_policy

- `num_actions`
- 策略向量 `p = np.ones(num_actions) / num_actions`（均匀固定策略）

---

## 4. DNN 相关参数总表

以下参数在 `NeuralBanditModel` / `NeuralLinear` / `NeuralUCB` / `NeuralLinUCB` 中反复出现：

- `layer_sizes`
- `activation`
- `initial_lr`
- `batch_size`
- `init_scale`
- `use_dropout`
- `dropout_rate`
- `layer_norm`
- `verbose`
- `context_dim`
- `num_actions`

算法类默认值（当外层未传时）以类内 `hparams.get(...)` 为准，典型默认：

- `layer_sizes=[100, 100]`
- `activation="relu"`
- `initial_lr=0.001`
- `batch_size=512`
- `init_scale=0.3`
- `use_dropout=False`
- `dropout_rate=0.1`
- `layer_norm=False`

---

## 5. OurMethod 参数总览

## 5.1 命令行参数（run_dataset_step.py 的 `ourmethod` 子命令）

- `--datasets` 默认 `['newsgroups']`
- `--n_rounds` 默认 `2000`
- `--cold_start_n` 默认 `50`
- `--seed` 默认 `42`
- `--model` (`model_name`) 默认 `smollm2`
- `--custom_model` 默认 `None`
- `--z_dim` 默认 `16`
- `--c_ucb1` 默认 `1.0`
- `--alpha_linucb` 默认 `1.0`
- `--c_sim` 默认 `1.0`
- `--offline_freq` 默认 `500`
- `--offline_epochs` 默认 `10`
- `--warmup_epochs` 默认 `15`
- `--no_cache` 标志位
- `--dump_prompts` 默认 `3`
- `--ablation_mode` 默认 `none`，可选: `none`, `s1_only`, `s2_only`, `s3_only`

## 5.2 主流程函数参数（OurMethod/run_full_experiment.py）

`run_our_method_pipeline(...)` 关键默认值：

- `model_name='stub'`
- `custom_model=None`
- `seed=42`
- `z_dim=16`
- `c_ucb1=1.0`
- `alpha_linucb=1.0`
- `c_sim=1.0`
- `offline_freq=500`
- `offline_epochs=10`
- `warmup_epochs=15`
- `verbose=True`
- `dataset_name='statlog'`
- `cold_start_tag=None`
- `no_cache=False`
- `ckpt=None`
- `arms=None`
- `prompt_builder=None`
- `dump_prompts=0`
- `ablation_mode='none'`
- `run_timestamp=None`

## 5.3 Prompt 构造参数（StructuredPromptBuilder）

默认值：

- `role='intelligent decision system'`
- `scenario='contextual bandit optimization'`
- `max_feedback=10`
- `feature_names=None`
- `arm_reward_range='0-1'`
- `max_feature_display=15`

在 `run_dataset_step.py` 中数据集特化实例：

- `newsgroups`:
  - `role='text topic classification expert'`
  - `scenario='20 Newsgroups topic classification as contextual bandit. Given TF-IDF/LSA semantic features of a document, predict which newsgroup topic it belongs to.'`
  - `max_feedback=5`
  - `max_feature_display=10`

- `ag_news`:
  - `role='news topic classification expert'`
  - `scenario='AG News topic classification as contextual bandit. Given the text of a news article, predict which topic it belongs to.'`
  - `max_feedback=5`
  - `max_feature_display=0`

## 5.4 LLM 注册与生成参数（build_registry + frozen_llm）

- 预置模型别名:
  - `stub` -> `StubFrozenLLM(hidden_dim=128, generate_ds_llm=True)`
  - `smollm2` -> `HuggingFaceTB/SmolLM2-360M-Instruct`
  - `qwen2_5_7b` -> `/home/csg/Awesome-contextual-bandits/models/huggingface/Qwen2.5-7B-Instruct`
  - `llama3_1_8b` -> `meta-llama/Llama-3.1-8B-Instruct`

- build_registry 默认生成相关参数:
  - `max_new_tokens=256`
  - `temperature=0.1`
  - `generate_ds_llm=True`
  - PyTorch 路径默认 `use_chat_template=True`

- GenerativeFrozenLLM 默认值:
  - `model_name='HuggingFaceTB/SmolLM2-360M-Instruct'`
  - `device=None`（自动检测）
  - `max_input_length=1024`
  - `max_new_tokens=384`
  - `temperature=0.1`
  - `use_chat_template=True`
  - `generate_ds_llm=True`
  - `use_half=True`
  - `system_prompt='You are an expert decision-making assistant. Always respond with valid JSON only, no extra text.'`

- MLXGenerativeFrozenLLM 默认值:
  - `model_name='HuggingFaceTB/SmolLM2-360M-Instruct'`
  - `max_input_length=1024`
  - `max_new_tokens=256`
  - `temperature=0.1`
  - `generate_ds_llm=True`
  - `system_prompt` 同上

## 5.5 特征压缩器参数（MLPCompressor）

默认值：

- `input_dim`（由 LLM hidden dim 决定）
- `output_dim=32`
- `hidden_dim=None`（自动推导）
- `dropout=0.1`
- `lr=1e-3`

在主流程中实际实例化为：

- `MLPCompressor(input_dim=h_dim, output_dim=z_dim)`

## 5.6 策略参数（CombinedUCBPolicy）

默认值：

- `num_actions`
- `z_dim`
- `c_ucb1=1.0`
- `alpha_linucb=1.0`
- `c_sim=1.0`
- `lambda_reg=1.0`
- `initial_pulls=1`
- `name='combined_ucb'`
- `ablation_mode='none'`

## 5.7 离线训练参数（OfflineTrainer）

默认值：

- 构造函数: `lr=1e-3`
- `train(...)`: `epochs=10`, `batch_size=64`

主流程中两处使用：

- 热启动: `OfflineTrainer(..., lr=1e-3)` + `train(..., epochs=warmup_epochs, batch_size=64)`
- 在线周期离线训练: `OfflineTrainer(..., lr=1e-3)` + `train(..., epochs=offline_epochs, batch_size=64)`

---

## 6. 备注与易混点

- `run_dataset_step.py` 的 `neural_ucb` 传的是 `exploration_bonus=0.1`，而 `NeuralUCBSampling` 类里使用的是 `alpha`（默认 1.0）。如果你希望该入口直接控制探索强度，建议改为传 `alpha`。
- `run_dataset_step.py` 与 `run_all_experiments.py` 对同名算法（例如 `ucb1` 的 `alpha`、`neural_ucb` 的训练频率）存在默认配置差异，做横向对比实验时建议统一。
- OurMethod 的外层默认模型在两个入口里不一致：
  - `run_dataset_step.py` 的 `ourmethod` 默认 `smollm2`
  - `run_full_experiment.py` 默认 `stub`
